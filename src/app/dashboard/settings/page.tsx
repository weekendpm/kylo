'use client'

import { DashboardNotFound } from '@/components/dashboard/not-found'

export default function SettingsPage() {
  return (
    <DashboardNotFound 
      title="Settings"
      description="Configure thresholds, notifications, and user permissions. Settings panel coming soon!"
    />
  )
}